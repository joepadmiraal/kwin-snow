## Summary

What does this change do, and why?

## Testing

- [ ] `cmake --build build`
- [ ] `ctest --test-dir build --output-on-failure`
- [ ] Nested KWin session tested with `tools/dev.sh --panel` (if relevant)
- [ ] Live Plasma session tested (if relevant)

## Notes

List the KWin/Qt/KDE Frameworks versions used for compositor testing.
